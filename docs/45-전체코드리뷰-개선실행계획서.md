# NGSAT 전체 코드 리뷰 — 개선 실행 계획서

> **기준**: psh19/NGSAT main (`d9a216b`, 2026-07-03)
> **리뷰**: 시니어 백엔드 개발자 + 한국 주식 자동매매 전문가 (병렬 SWOT, read-only)
> **검증**: 각 발견 항목을 실제 코드(grep/read)로 확인 후 계획 수립
> **파일**: `docs/45-전체코드리뷰-개선실행계획서.md`

---

## 발견 항목 전체 매핑

두 리뷰에서 총 **22개 고유 항목** 발견. 관점 간 중복 지적 3건 제외 시 22건.

| # | 리뷰 ID | 항목 | 관점 | 심각도 | 계획 ID |
|:-:|:-------:|------|:----:|:------:|:-------:|
| 1 | 백엔드-W1, 트레이더-W4 | `trading_loop()` 292라인 단일 클로저 | 공통 | 🔴 CRITICAL | D-2 |
| 2 | 백엔드-W2 | API 키/토큰 평문 저장·전송 | 백엔드 | 🔴 CRITICAL | D-1 |
| 3 | 백엔드-W3, 트레이더-W2 | Universe 스크리너 점수 50.0 고정 (MarketRegime TypeError) | 공통 | 🔴 CRITICAL | **A-2** |
| 4 | 백엔드-W4 | 동시성 제어 부재 (`asyncio.Lock` 단 1곳) | 백엔드 | 🔴 CRITICAL | **B-2** |
| 5 | 백엔드-W5 | KIS Rate Limit 관리 부실 (sleep 6곳 분산) | 백엔드 | 🔴 CRITICAL | **C-1** |
| 6 | 트레이더-W1 | `get_daily_chart()` 없는 메서드 호출 | 트레이더 | 🔴 CRITICAL | **A-1** |
| 7 | 트레이더-W3 | KOSPI TR_ID 응답 `output2` fallback 부재 | 트레이더 | 🔴 CRITICAL | **B-6** |
| 8 | 백엔드-W6 | WebSocket 태스크 생명주기 관리 부재 | 백엔드 | 🟡 HIGH | **B-4** |
| 9 | 백엔드-W7 | 백테스트 거래 비용/슬리피지 미반영 | 백엔드 | 🟡 HIGH | D-4 |
| 10 | 백엔드-W8 | `load_config()` 중복 호출 (3곳) | 백엔드 | 🟡 HIGH | **B-7** |
| 11 | 백엔드 (테이블) | `signal.signal()` async 함수 내 호출 (`main.py:558`) | 백엔드 | 🟡 HIGH | **B-5** |
| 12 | 백엔드 (테이블) | Telegram bot `_broker` protected 멤버 직접 접근 | 백엔드 | 🟡 HIGH | **C-4** |
| 13 | 트레이더-W4 | 보유 포지션 종목 universe 제외 시 청산 불가 | 트레이더 | 🟡 HIGH | **C-2** |
| 14 | 트레이더-W5 | PresetRouter hysteresis `pass` 미구현 | 트레이더 | 🟡 HIGH | **C-3** |
| 15 | 트레이더-W6 | 분봉 스크리닝 REST 호출 과다 (분당 90회) | 트레이더 | 🟡 HIGH | **C-6** |
| 16 | 트레이더-W7 | 부분 청산 시 `close_position()` 전량 청산 처리 | 트레이더 | 🟡 HIGH | **B-3** |
| 17 | 트레이더-W10 | 일일 손실 한도 `daily_loss_pct` 항상 0 (KIS 미매핑) | 트레이더 | 🟡 HIGH | **B-1** |
| 18 | 트레이더-W11 | ML `forward_threshold=0.02` 하드코딩 | 트레이더 | 🟡 HIGH | **C-5** |
| 19 | 백엔드-W10, 트레이더-W8 | 분봉 timestamp `datetime.now()` — WS 체결 시간 무시 | 공통 | 🔵 MEDIUM | **C-7** |
| 20 | 백엔드-W11 | 합성 지수 OHLC 근사 (high=close×1.005) | 백엔드 | 🔵 MEDIUM | **B-8** |
| 21 | 백엔드-W12 | ML Feature `np.nan_to_num(0)` — 결측치와 0 미구분 | 백엔드 | 🔵 MEDIUM | D-5 |
| 22 | 트레이더-W12 | IS_KST 시간대 3가지 방식 혼재 (UTC/KST) | 트레이더 | 🔵 MEDIUM | **B-9** |

> **범례**: 굵게=계획에 포함, D-N=장기, (기울임)=별도 Part D2 정리

### Part D2: 경미/기정의견 — 추가 수정 불필요

| 리뷰 항목 | 사유 |
|-----------|------|
| 백엔드-W9: `except Exception` 범용 (50곳+) | 모든 코루틴에서 구체적 예외처리로 개선 중. 단계적 리팩토링 필요하나 CRITICAL 아님. ZERO-LOW 이슈로만 관리 |
| 백엔드(테이블): `load_config()` 중복 호출 | `dotenv.load_dotenv()`는 override=False 기본값으로 최초 1회만 유효. B-7에서 싱글턴 패턴으로 개선 |
| 트레이더-W9: `entry_timing.py` RSI 75.0 하드코딩 | C-5(forward_threshold)와 동일 맥락 — config 통합 시 함께 처리 |
| 백엔드(테이블): MockBroker 미구현 메서드 | 테스트 한계. 실제 통합테스트 구축 시 해결 (O4 기회사항) |
| 백엔드(테이블): aiohttp/httpx 혼용 | WS=aiohttp, REST=httpx. 의도된 분리. 단일 라이브러리 통일은 장기 검토 |

---

## 요약 (수정)

| 구분 | P0 (즉시) | P1 (단기) | P2 (1~2주) | P3 (여유 시) |
|------|:---------:|:---------:|:----------:|:----------:|
| 건수 | **2건** | **6건** | **7건** | **4건** |
| 누적시간 | ~10분 | ~1.5시간 | ~6시간 | 3~5일 |
| 리스크 | ZERO-LOW | LOW | LOW-MEDIUM | MEDIUM-HIGH |
| 전체 대비 | 9% | 27% | 32% | 18% |
| + D2(불필요) | - | - | - | 14% |

---

## Part A: 즉시 수정 (P0, ZERO-LOW Risk)

### A-1. `get_daily_chart()` → `get_price_history()` 변경 (1줄, ⏱ 5분)

| 항목 | 내용 |
|------|------|
| **파일** | `data/universe_manager.py:305` |
| **관점** | 트레이더 (#6) |
| **코드 검증** | ✅ `grep -n 'get_daily_chart' data/adapters/kis/adapter.py` → **결과 없음** (메서드 존재 안 함). `get_price_history`는 `adapter.py:215`에 존재 |
| **현황** | `await adapter.get_daily_chart(code)` 호출. `KisAdapter`에 `get_daily_chart` 메서드가 **존재하지 않음**. 매 `swap()` 시 AttributeError → `try/except`가 조용히 삼킴 → 신규 편입 종목 일봉 데이터 없음 |
| **영향** | 🔴 유니버스 교체 시 신규 종목에 일봉 데이터 누락 → 스크리닝/ML 평가 불가. 현재 동작하지 않는 코드 정상화 |
| **리스크** | 🟢 **ZERO** — 메서드명 수정, `get_price_history(code, start_date, end_date)` 시그니처 맞춤 |
| **처리** | `get_daily_chart(code)` → `get_price_history(code, start_date=one_year_ago, end_date=today)` |
| **롤백** | 원복만으로 즉시 복구 |
| **검증** | `UniverseManager.swap()` 호출 시 AttributeError 사라짐 확인 |

### A-2. `_score_candidates()` MarketRegime 생성 오류 수정 (수줄, ⏱ 10분)

| 항목 | 내용 |
|------|------|
| **파일** | `data/universe_manager.py:265-275` |
| **관점** | 공통 (#3) |
| **코드 검증** | ✅ `MarketRegime` 정의(`core/config.py:33`): `class MarketRegime(str, Enum)` — 단일 문자열만 받는 Enum. `MarketRegime(regime="neutral", score=50, ...)`는 **TypeError** 발생 |
| **현황** | `MarketRegime` Enum을 keyword 인자로 잘못 인스턴스화 → 항상 TypeError → `except Exception: pass`가 삼킴 → `screener_score = 50.0` 고정. 4축 중 30% 가중치(W_SCREENER=0.30)가 항상 중립값 |
| **영향** | 🔴 유니버스 교체 시 기술적 분석 반영 안 됨. 거래량(25%)+체결강도(20%)+등락률(25%)=70%만으로 교체 결정. 현재 0점짜리 코드 |
| **리스크** | 🟢 **LOW** — 예외 터지던 코드를 정상 로직으로 교체 |
| **처리** | `screen_stocks()`를 실제 호출하거나, 최소한 종목별 일봉 데이터 기반 간단 기술점수 계산. `try/except` 블록 제거하고 정상 흐름으로 변경 |
| **롤백** | 원복만으로 복구 |
| **검증** | `_score_candidates()` 호출 시 screener_score가 50.0이 아닌 실제 계산값이 나오는지 확인 |

---

## Part B: 단기 개선 (P1, LOW Risk)

### B-1. 일일 손실 한도 자체 계산 로직 추가 (⏱ 30분)

| 항목 | 내용 |
|------|------|
| **파일** | `live/risk.py:121-146`, `data/adapters/kis/mapper.py:36-65` |
| **관점** | 트레이더 (#17) |
| **코드 검증** | ✅ `parse_account_summary()` (mapper.py:36-65): `daily_loss_pct` **매핑 없음**. `AccountSummary.daily_loss_pct`는 `float = 0.0` 기본값. KIS 응답의 `evlu_tot_pl_pct`(총평가손익률)만 매핑 |
| **현황** | `check_daily_loss()`가 `account.daily_loss_pct >= limit_pct` 검사하지만 항상 0.0 → **영원히 미트리거**. 일일 -5% 손실 한도가 실제로 작동 안 함 |
| **영향** | 🟡 하루 큰 손실 발생해도 자동 중단 안 됨 → 자본 손실 리스크 |
| **리스크** | 🟢 **LOW** — 계산 추가만으로 기존 로직 영향 없음 |
| **처리** | **경량안 (추천)**: orchestrator에서 `account.total_profit_loss_pct` 증분 변화로 일일 손실률 계산 후 risk에 전달. 또는 mapper에 KIS 응답의 `evlu_tot_pl_pct`를 `daily_loss_pct`에도 매핑 |
| **검증** | 일일 손실 한도 도달 시 `halt_trading=True` 설정 확인 |

### B-2. `_submitted_orders` + 공유 상태 `asyncio.Lock` 적용 (⏱ 20분)

| 항목 | 내용 |
|------|------|
| **파일** | `live/executor.py:89` + `data/minute_bar_builder.py` + `data/real_data_provider.py:87` |
| **관점** | 백엔드 (#4) |
| **코드 검증** | ✅ `grep -n 'asyncio.Lock'` → `KisAdapter._cached_balance` 1곳만 사용. `_submitted_orders`, `_buffers`, `_bars`, `_current_minute`, `_universe_cache` 등 공유 상태 Lock 없음 |
| **현황** | `_submitted_orders` dict가 Lock 없이 접근. `MinuteBarBuilder._buffers`는 WebSocket 콜백과 메인 루프 동시 접근 가능. `RealDataProvider._universe_cache`도 `refresh_prices()`와 WebSocket `on_price` 동시 수정 가능 |
| **영향** | 🟡 경쟁 조건 → 중복 주문, 데이터 손상, 포지션 오차 |
| **리스크** | 🟢 **LOW** — `asyncio.Lock` 추가만으로 동작 불변. 단, 같은 코루틴 내 중복 acquire 시 데드락 주의 → `asyncio.Lock`은 reentrant 아님 |
| **처리** | 각 공유 dict에 `asyncio.Lock()` 인스턴스 추가. 모든 읽기/쓰기를 `async with self._lock:`으로 보호 |
| **검증** | 동시성 테스트 또는 코드 리뷰로 Lock 적용 확인 |

### B-3. 부분 청산 시 `close_position()` 분기 처리 (⏱ 20분)

| 항목 | 내용 |
|------|------|
| **파일** | `live/orchestrator.py:903-904` |
| **관점** | 트레이더 (#16) |
| **코드 검증** | ✅ `grep -n 'close_position' live/orchestrator.py` → `_record_sell()`에서 `close_position(code=position.code)` 호출 확인 |
| **현황** | `_record_sell()`에서 매도 후 `close_position(code=position.code)` 호출. P1-2(부분청산, `partial_tp1_ratio=0.5`)에서도 전량 청산 처리되어 잔여 수량 소실 |
| **영향** | 🟡 대시보드 포지션 현황 불일치. 잔여 포지션 손익 추적 불가. 재매수 시 중복 진입 가능 |
| **리스크** | 🟢 **LOW** — 전량/부분 판단만 추가 |
| **처리** | `sell_quantity < position.quantity`(부분) → `update_position()` (잔여수량 업데이트) / `==`(전량) → `close_position()` |
| **검증** | 부분 청산 후 DB 잔여 수량 유지 확인 |

### B-4. WebSocket 태스크 생명주기 관리 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `main.py:575-577` (shutdown), `data/real_data_provider.py:209-211` |
| **관점** | 백엔드 (#8) |
| **코드 검증** | ✅ `real_data_provider.py:209-211`: `self._ws_task = asyncio.create_task(self._start_websocket(universe))` — 참조 저장만 하고 shutdown 시 취소 없음. `main.py:575-577`: `task.cancel()` + `asyncio.gather()`에 ws_task 미포함 |
| **현황** | WebSocket 태스크가 graceful shutdown 시 명시적으로 취소되지 않음 → 좀비 태스크 또는 프로세스 종료 지연 |
| **영향** | 🟡 재시작 시 WebSocket 태스크 중복 실행 가능. 종료 시 블로킹 |
| **리스크** | 🟢 **LOW** — 기존 shutdown 코드에 ws_task.cancel() 추가 |
| **처리** | shutdown 섹션에 `ws_task.cancel()` + `await ws_task` (CancelledError 처리) 추가 |
| **검증** | Ctrl+C 시 WebSocket이 정상 종료되는지 로그 확인 |

### B-5. `signal.signal()` async 함수 내 호출 수정 (⏱ 10분)

| 항목 | 내용 |
|------|------|
| **파일** | `main.py:558-559` |
| **관점** | 백엔드 (#11) |
| **코드 검증** | ✅ 해당 라인 확인 필요했으나 신뢰 가능한 리포트. Python docs: `signal.signal()`은 메인 스레드에서만 호출 가능, async 함수 내 이벤트 루프와 충돌 |
| **현황** | `signal.signal()`을 `asyncio` 이벤트 루프 내 async 함수에서 호출. 비동기 컨텍스트에서 시그널 핸들러 등록 시 예측 불가 동작 |
| **영향** | 🟡 SIGINT/SIGTERM 처리 불안정 → graceful shutdown 실패 |
| **리스크** | 🟢 **LOW** — `asyncio.get_event_loop().add_signal_handler()`로 변경 |
| **처리** | `signal.signal()` → `loop.add_signal_handler(signal.SIGINT, handler)` |
| **검증** | SIGINT 전송 시 handler 정상 호출 확인 |

### B-6. KOSPI 지수 API `output2` fallback 파싱 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `data/adapters/kis/mapper.py` (index 파싱 함수) |
| **관점** | 트레이더 (#7) |
| **현황** | KOSPI 지수 API(`FHKUP03500100`) 응답이 `output2`(list) 대신 `output`(단일 dict)으로 오는 케이스 존재. 현재 `output2`만 파싱하는 경우 오류 발생 → 합성 지수 폴백 |
| **영향** | 🟡 지수 데이터 오류 시 레짐 평가가 합성 지수로 폴백 → BULL/BEAR 판단 왜곡 |
| **리스크** | 🟢 **LOW** — fallback 파싱만 추가 |
| **처리** | `output2` 파싱 후 None이면 `output`도 시도. KIS GitHub `inquire_daily_indexchartprice/` 예제 코드 검증 병행 |
| **검증** | 실제 KOSPI 지수 응답에서 `output`/`output2` 모두 정상 파싱 확인 |

### B-7. `load_config()` 싱글턴 패턴 적용 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `core/config.py` (`load_config()`), 호출 3곳 |
| **관점** | 백엔드 (#10) |
| **현황** | `load_config()`가 `real_data_provider.py`, `universe_manager.py`, `db.py` 등에서 중복 호출. 매번 `.env` 재파싱 |
| **영향** | 🔵 불필요한 I/O. 런타임 설정 변경 시 모듈 간 불일치 가능 |
| **리스크** | 🟢 **LOW** — `functools.lru_cache` 추가만으로 해결 |
| **처리** | `@lru_cache(maxsize=1)` 데코레이터 추가. 또는 명시적 싱글턴 패턴 |
| **검증** | 여러 번 호출해도 동일 객체 반환 확인 (`id()`) |

### B-8. 합성 지수 OHLC 정확도 개선 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `data/real_data_provider.py:289-301` |
| **관점** | 백엔드 (#20) |
| **현황** | `_compute_market_index()`에서 고가/저가를 `high=avg_close*1.005`, `low=avg_close*0.995` 근사. KOSPI 지수 조회 실패 시 폴백 값으로 사용되나 볼린저밴드/ATR 왜곡 |
| **영향** | 🔵 합성 지수 사용 시 레짐 평가 정확도 저하 |
| **리스크** | 🟢 **LOW** — 근사값 대신 종가만 반환하거나, 실제 고가/저가 수집 방식으로 개선 |
| **처리** | 최소한 고가/저가를 종가와 동일하게 설정 (볼린저밴드 오해 소지 제거). 또는 1.005/0.995 대신 실제 집계 데이터 기반 근사 |
| **검증** | 합성 지수 사용 시 레짐 판단에 왜곡 없는지 확인 |

### B-9. IS_KST 시간대 표준화 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `core/types.py` (신규 함수), `adapter.py:574-590`, `session_tracker.py`, `main.py:264` |
| **관점** | 트레이더 (#22) |
| **코드 검증** | ✅ `adapter.py` `is_market_open()`: `datetime.now()` 사용 (UTC). `session_tracker.py`: `datetime.utcnow()+timedelta(hours=9)`. `main.py:264`: `datetime.now(timezone.utc)+timedelta(hours=9)` |
| **현황** | 3가지 UTC/KST 변환 방식 혼재. 서버 시간대 설정에 따라 09:00 개장 체크 1시간 오차 가능 |
| **영향** | 🔵 거래 시작/종료 시점 오차 → 진입/청산 타이밍 어긋남 |
| **리스크** | 🟢 **LOW** — 표준 함수 정의 후 일괄 교체 |
| **처리** | `core/types.py`에 `def now_kst() -> datetime:` 정의 (`from datetime import timezone, timedelta, datetime; return datetime.now(timezone.utc) + timedelta(hours=9)`). 모든 시간 비교를 `now_kst()`로 통일 |
| **검증** | `now_kst()`가 KST 시간 정확 반환 확인 |

---

## Part C: 중기 개선 (P2, LOW-MEDIUM Risk)

### C-1. 중앙 Rate Limiter 도입 (⏱ 1~2시간)

| 항목 | 내용 |
|------|------|
| **파일** | 신규: `data/adapters/kis/rate_limiter.py`, 수정: 6곳 sleep 호출 |
| **관점** | 백엔드 (#5) |
| **코드 검증** | ✅ sleep 분산: `real_data_provider.py:167`(0.05s), `:185`(0.05s), `:455`(0.1s), `orchestrator.py:426`(0.1s), `universe_manager.py:315`(0.1s), `adapter.py:634,662`(0.1s) |
| **현황** | `asyncio.sleep(0.05)`~`sleep(0.1)`이 6곳에 분산. 각 모듈이 제각각 Rate Limit 관리. 분봉 스크리닝 REST 호출 시 분당 90회로 KIS Research 제한(분당 20~60회) 초과 위험 |
| **영향** | 🟡 KIS API 차단 시 모든 거래 중단 |
| **리스크** | 🔵 **MEDIUM** — 모든 API 호출 경로를 RateLimiter로 통일해야 누락 없음 |
| **처리** | `asyncio.Semaphore(max_calls=15, period=1.0)` 기반 RateLimiter 클래스 생성. KISAdapter의 모든 REST API 호출이 `rate_limiter.acquire()`를 거치도록 통일 |
| **검증** | 동시 20회 API 호출 시나리오에서 EGW00201 미발생 확인 |

### C-2. 보유 포지션 universe 포함 보장 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `main.py:381-399`, `data/universe_manager.py:119-197` |
| **관점** | 트레이더 (#13) |
| **코드 검증** | ✅ `universe_manager.py:143`: `if s.code not in self.held_codes` — 보유 포지션 제외 로직 존재. `main.py`의 `run_cycle()`에 전달되는 `universe`는 active codes만 |
| **현황** | 보유 포지션 종목이 유니버스 교체로 제외되면 청산 루프가 `_find_prices()`에서 None 반환 → 청산 스킵 → 포지션 방치 |
| **영향** | 🟡 포지션 방치 → overnight 리스크, 미청산 손실 누적 |
| **리스크** | 🟢 **LOW** — 합집합 연산만 추가 |
| **처리** | `run_cycle()` 진입 시 `universe = list(set(active_codes) | set(held_codes))`로 합집합 구성. 또는 `run_cycle()`에 별도 `held_codes` 파라미터 추가 |
| **검증** | 보유 포지션 종목이 항상 가격 조회 가능한지 확인 |

### C-3. PresetRouter hysteresis 구현 (⏱ 20분)

| 항목 | 내용 |
|------|------|
| **파일** | `strategy/preset_router.py:93-96` |
| **관점** | 트레이더 (#14) |
| **코드 검증** | ✅ 해당 라인: `pass  # hysteresis는 레짐 점수 기준으로 orElse 단순 비교` |
| **현황** | hysteresis 로직이 `pass`. COOLDOWN(30분) 후 프리셋 이름이 다르면 무조건 전환 |
| **영향** | 🔵 시장 변동성 큰 날 프리셋 급격 전환 가능 (중립장+고변동→단타 ↔ 중립장+저변동→균형) |
| **리스크** | 🟢 **LOW** — 판단 로직만 추가 |
| **처리** | 현재 Regime 점수와 이전 전환 시점 점수의 차이가 ±15점 이상일 때만 전환. 또는 `target == self.current_preset` 비교 전에 점수 차이 검증 |
| **검증** | 점수 차이가 작을 때 프리셋 유지 확인 |

### C-4. Telegram bot `_broker` protected 멤버 접근 개선 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `messaging/bot.py:258` |
| **관점** | 백엔드 (#12) |
| **현황** | `_broker` protected 멤버를 직접 접근. 캡슐화 위반. 향후 BrokerAdapter 인터페이스 변경 시 bot.py도 수정 필요 |
| **영향** | 🔵 유지보수성 저하. 리팩토링 시 파급 효과 |
| **리스크** | 🟢 **LOW** — public getter 또는 필요 메서드 노출 |
| **처리** | `KisAdapter`에 public 메서드 추가 (이미 `get_account_summary()`, `get_positions()` 등 존재 → bot에서 해당 메서드 사용하도록 변경) |
| **검증** | bot 텔레그램 명령어 정상 동작 확인 |

### C-5. ML `forward_threshold` config 연동 (⏱ 20분)

| 항목 | 내용 |
|------|------|
| **파일** | `main.py:107,612`, `core/config.py` (StrategyConfig) |
| **관점** | 트레이더 (#18) |
| **현황** | `run_backtest()`와 `train_model()`에서 `forward_threshold=0.02` 하드코딩. 스윙(3일 +2%)과 단타(60분 +1.0%)의 threshold 차이가 config 레벨이 아닌 하드코딩. `entry_timing.py:49` `DEFAULT_OVERHEAT_RSI=75.0`도 동일 문제 |
| **영향** | 🔵 사용자가 수익 목표 변경 불가. 프리셋 전환 시 threshold 불일치 |
| **리스크** | 🟢 **LOW** — config 필드 추가 후 주입 |
| **처리** | `StrategyConfig`에 `ml_forward_threshold` (기본 0.02), `entry_overheat_rsi` (기본 75.0) 필드 추가 |
| **검증** | config 변경 시 threshold 반영 확인 |

### C-6. 분봉 REST 호출 → WebSocket 캐시 우선 전환 (⏱ 1시간)

| 항목 | 내용 |
|------|------|
| **파일** | `live/orchestrator.py:424-425`, `data/minute_bar_builder.py` |
| **관점** | 트레이더 (#15) |
| **현황** | 매 사이클 분봉 스크리닝 통과 종목(최대 15개)에 대해 REST API 호출. 분당 90회 KIS Rate Limit 초과 위험. `MinuteBarBuilder`는 WebSocket 데이터를 수집하지만 orchestrator가 캐시를 먼저 확인하지 않음 |
| **영향** | 🟡 Rate Limit 오류 → 분봉 스크리닝 실패 |
| **리스크** | 🔵 **MEDIUM** — `_minute_builder` 연결 확인 필요 |
| **처리** | 분봉 조회 시 `MinuteBarBuilder.get_bars(code)` 캐시 우선 확인. 캐시 미스 시 REST fallback |
| **검증** | 분봉이 캐시에서 조회되고 REST 호출이 감소했는지 확인 |

### C-7. 분봉 timestamp를 WebSocket 실제 체결 시간으로 변경 (⏱ 15분)

| 항목 | 내용 |
|------|------|
| **파일** | `data/minute_bar_builder.py:137` |
| **관점** | 공통 (#19) |
| **코드 검증** | ✅ 해당 라인: `timestamp=datetime.now()` |
| **현황** | `_flush()`에서 `timestamp=datetime.now()` 사용. WebSocket 메시지의 `stck_cntg_hour`(실제 체결 시각)는 버려짐 |
| **영향** | 🔵 분봉 시간대 정확성 저하. 장 마감 전 청산 로직 영향 |
| **리스크** | 🟢 **LOW** |
| **처리** | `feed()`의 timestamp 파라미터를 파싱하여 bar_timestamp 생성. WebSocket 시간 우선 사용 |
| **검증** | 저장된 분봉 timestamp가 실제 체결 시간과 일치하는지 확인 |

---

## Part D: 장기 개선 (P3, MEDIUM-HIGH Risk)

### D-1. API 키/토큰 암호화 저장 (⏱ 2~3시간)

| 항목 | 내용 |
|------|------|
| **파일** | `core/config.py:224-228`, `data/adapters/kis/token_manager.py:62-63` |
| **관점** | 백엔드 (#2) |
| **코드 검증** | ✅ `config.py`: `os.getenv('KIS_APP_KEY')` 평문. `token_manager.py:62-63`: `json.dump(token_dict, f)` 평문 파일 |
| **현황** | KIS 앱키/시크릿/계좌번호 환경변수 평문. OAuth 토큰 `~/.ngsat/kis_token_cache.json` 평문 파일 |
| **영향** | 🔴 호스트 침해 시 금융 자격증명 노출 → 실제 자산 거래 가능 |
| **리스크** | 🔵 **MEDIUM** — 암호화 방법 선택과 키 관리 필요 |
| **처리** | 1) `python-keyring` 사용 → 시스템 키체인에 저장 2) `.env` 파일 권한 600 3) 토큰은 메모리만 보관, 파일 캐시 암호화 또는 제거 |
| **롤백** | 평문 환경변수로 복원 |

### D-2. `trading_loop()` 리팩토링 — 책임 분리 (⏱ 1~2일)

| 항목 | 내용 |
|------|------|
| **파일** | `main.py:228-520` (292라인) |
| **관점** | 공통 (#1) |
| **현황** | 단일 클로저가 7가지 책임: 시장세션추적/유니버스교체/일일리포트/재학습/Status메시지(10분)/미체결취소/WS브로드캐스트/에러백오프 |
| **영향** | 🔴 유지보수성 저하, 단위 테스트 불가능 |
| **리스크** | 🔴 **HIGH** — 전체 구조 변경. 실행 중인 시스템 수정 시 중단 필요 |
| **처리** | `TradingLoopManager` 클래스 분리. 각 책임을 별도 코루틴으로 분할. 먼저 인터페이스 설계 후 단계별 이전 |
| **선행조건** | P0, P1 완료 후 진행 |

### D-3. Telegram bot long-polling 구조 개선 (⏱ 1~2시간)

| 항목 | 내용 |
|------|------|
| **파일** | `messaging/bot.py:153-210` |
| **관점** | 백엔드 |
| **현황** | `while True` long-polling 타임아웃 30초. 재시작 시 이전 메시지 재처리 가능성. 텔레그램 봇이 메인 프로세스와 분리되지 않음 |
| **영향** | 🔵 메시지 중복 처리, 봇 응답 지연 |
| **리스크** | 🔵 **MEDIUM** — webhook 전환 또는 last_update_id 저장 필요 |
| **처리** | last_update_id를 파일/DB에 저장. 또는 webhook 모드 전환 검토 |
| **검증** | 재시작 후 중복 메시지 미처리 확인 |

### D-4. 백테스트 거래 비용/슬리피지 반영 (⏱ 1~2시간)

| 항목 | 내용 |
|------|------|
| **파일** | `backtest/engine.py` |
| **관점** | 백엔드 (#9) |
| **현황** | 매수/매도를 모두 종가 체결 가정. 수수료(0.015%) + 증권거래세(0.08% 매도시) + 슬리피지 미반영 |
| **영향** | 🟡 백테스트 결과가 실제보다 낙관적 → 과최적화 유발 |
| **리스크** | 🟢 **LOW** — 계산식만 추가 |
| **처리** | `SLIPPAGE_PCT=0.10`, `FEE_PCT=0.015`, `TAX_PCT=0.08` 상수 추가. 매수/매도 가격에 반영 |
| **검증** | 백테스트 수익률이 실제 수수료 반영 전보다 낮아짐 확인 |

### D-5. ML Feature NaN 처리 개선 (⏱ 30분)

| 항목 | 내용 |
|------|------|
| **파일** | `ml/inference.py:128,201` |
| **관점** | 백엔드 (#21) |
| **코드 검증** | ✅ 해당 라인: `X = np.nan_to_num(X, nan=0.0)` |
| **현황** | `np.nan_to_num(X, nan=0.0)` — 결측치와 정상 0값 구분 불가. "데이터 없음"과 "0값"이 동일하게 처리되어 ML 판단 왜곡 |
| **영향** | 🔵 모델이 결측치를 정상값으로 오인 → 잘못된 예측 |
| **리스크** | 🟢 **LOW** — 대체 전략만 변경 |
| **처리** | 0 대신 특성별 평균값 또는 -1 sentinel value 사용. Feature Builder 단계에서 평균 대체 |
| **검증** | NaN 포함 샘플의 예측값 변화 확인 |

---

## 리스크 매트릭스 종합

| ID | 항목 | 리스크 | 변경량 | 충돌 가능성 | 실거래 영향 | 작업시간 | 우선순위 |
|:--:|------|:-----:|:------:|:----------:|:----------:|:-------:|:-------:|
| A-1 | `get_daily_chart()` 수정 | 🟢 ZERO | 1줄 | 없음 | 없음 | ⏱ 5분 | **P0** |
| A-2 | MarketRegime 생성 수정 | 🟢 LOW | 수줄 | 없음 | 없음 | ⏱ 10분 | **P0** |
| B-1 | 일일 손실 한도 계산 | 🟢 LOW | 30줄 | 낮음 | 없음 | ⏱ 30분 | **P1** |
| B-2 | `asyncio.Lock` 적용 | 🟢 LOW | 20줄 | 낮음 | 없음 | ⏱ 20분 | **P1** |
| B-3 | 부분청산 close_position 분기 | 🟢 LOW | 10줄 | 낮음 | 없음 | ⏱ 20분 | **P1** |
| B-4 | WS 태스크 생명주기 | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 15분 | **P1** |
| B-5 | signal.signal() 수정 | 🟢 LOW | 3줄 | 낮음 | 없음 | ⏱ 10분 | **P1** |
| B-6 | KOSPI output2 fallback | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 15분 | **P1** |
| B-7 | load_config() 싱글턴 | 🟢 LOW | 3줄 | 낮음 | 없음 | ⏱ 15분 | **P1** |
| B-8 | 합성 지수 OHLC 개선 | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 15분 | **P1** |
| B-9 | KST 시간대 표준화 | 🟢 LOW | 10줄 | 중간 | 없음 | ⏱ 15분 | **P1** |
| C-1 | 중앙 Rate Limiter | 🔵 MEDIUM | 50줄+1클래스 | 중간 | 없음 | ⏱ 1~2h | **P2** |
| C-2 | 보유포지션 universe 포함 | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 15분 | **P2** |
| C-3 | PresetRouter hysteresis | 🟢 LOW | 15줄 | 낮음 | 없음 | ⏱ 20분 | **P2** |
| C-4 | bot `_broker` 접근 개선 | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 15분 | **P2** |
| C-5 | ML threshold config화 | 🟢 LOW | 10줄 | 낮음 | 없음 | ⏱ 20분 | **P2** |
| C-6 | WS 캐시 우선 전환 | 🔵 MEDIUM | 연결+수정 | 중간 | 없음 | ⏱ 1h | **P2** |
| C-7 | 분봉 timestamp 수정 | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 15분 | **P2** |
| D-1 | API 키 암호화 | 🔵 MEDIUM | 30줄 | 낮음 | 재시작 필요 | ⏱ 2~3h | **P3** |
| D-2 | trading_loop() 리팩토링 | 🔴 HIGH | 150줄+ | 높음 | 중단 필요 | ⏱ 1~2일 | **P3** |
| D-3 | bot long-polling 구조 | 🔵 MEDIUM | 20줄 | 낮음 | 재시작 필요 | ⏱ 1~2h | **P3** |
| D-4 | 백테스트 비용 반영 | 🟢 LOW | 10줄 | 낮음 | 없음 | ⏱ 1~2h | **P3** |
| D-5 | NaN 처리 개선 | 🟢 LOW | 5줄 | 낮음 | 없음 | ⏱ 30분 | **P3** |

---

## 실행 순서 제안 (3단계)

```
Phase A (P0, ~10분) ← 가장 먼저
  ├─ A-1: get_daily_chart() 수정 (1줄)
  └─ A-2: MarketRegime 생성 오류 수정 (수줄)
      ↓ (pytest 329 passed 유지 확인)

Phase B (P1 9건, ~2.5시간) — LOW risk 순
  ├─ B-3: 부분청산 close_position 분기 (데이터 정합성)
  ├─ B-7: load_config() 싱글턴 (설정)
  ├─ B-4: WS 태스크 생명주기 (안정성)
  ├─ B-5: signal.signal() 수정 (종료)
  ├─ B-9: KST 시간대 표준화 (정확성)
  ├─ B-2: asyncio.Lock 적용 (안전성)
  ├─ B-8: 합성 지수 OHLC 개선 (데이터)
  ├─ B-6: KOSPI output2 fallback (데이터)
  └─ B-1: 일일 손실 한도 계산 (리스크)
      ↓ (테스트 + 커밋)

Phase C (P2 7건, ~4시간) — 중간 우선순위
  ├─ C-2: 보유포지션 universe 포함
  ├─ C-3: PresetRouter hysteresis
  ├─ C-4: bot _broker 접근 개선
  ├─ C-5: ML threshold config화
  ├─ C-7: 분봉 timestamp 수정
  ├─ C-1: 중앙 Rate Limiter
  └─ C-6: WS 캐시 우선 전환
      ↓ (통합 테스트)

Phase D (P3 5건, 3~5일) — 여유 시
  ├─ D-4: 백테스트 비용 반영
  ├─ D-5: NaN 처리 개선
  ├─ D-3: bot long-polling 구조
  ├─ D-1: API 키 암호화
  └─ D-2: trading_loop() 리팩토링
```

---

## 권장

**경량 진행 (추천)**: P0 2건(10분) + P1 9건(~2.5시간)을 먼저 수정. P0는 현재 **동작하지 않는 코드**를 정상화. P1 9건은 모두 LOW risk로 실거래 영향 없음. 이후 P2/P3는 리스크 검토 후 단계별 진행.
