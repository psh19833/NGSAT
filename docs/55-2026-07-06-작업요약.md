# NGSAT 2026-07-06 작업 요약

**커밋 범위**: `8ad8af8` ~ `fc0a1b8` (18 commits)  
**GitHub**: psh19833/NGSAT (main)  
**서버 상태**: running (커밋 `fc0a1b8`)

---

## 1. 스크리너 개선 (오전)

### 문제: 스크리너 통과 1종목

스크리너가 40종목 중 1종목(삼성전자)만 통과. reserve 60종목은 데이터 부재로 스크리닝 대상에서 제외됨.

### 해결: P-51 + P-52

| 커밋 | 내용 | 효과 |
|:-----|:------|:------|
| `65db272` | A안: UniverseManager reserve 캐시 동기화 + C안: Refresh 300→60 | reserve 종목도 스크리닝 대상에 포함 |
| `aacd697` | F안: DEFAULT_UNIVERSE_CODES(40) 베이스 + ranking 보충 하이브리드 | 유니버스 최소 40종목 보장 |

**결과**: 스크리닝 통과 **1개 → 9개**

---

## 2. 텔레그램 일일 보고서 수정 (오후)

### 문제: 일일 보고서 미발송 + 손익 0원

`core.db` 모듈 없음 → ImportError. 계좌 평가손익(보유 포지션 없으면 0) 사용.

### 해결: P-53

| 커밋 | 내용 | 효과 |
|:-----|:------|:------|
| `69fa7cb` | `core.db` import → `orchestrator._Session()` 대체 | 일일 보고서 전송 복구 |
| `2c8203c` | 손익 계좌평가→체결 기반 매칭 계산 | HLB +0원→+500원 |
| `91a09c8` | 체결가 제출시점→실제 체결가 조회 + Status 장 마감 후 차단 | 정확도 + Telegram 정리 |

---

## 3. 코드 리뷰 버그 수정 (저녁)

### 기반: NGSAT 전체 코드 리뷰 보고서 (18건)

### 처리 기준: 실거래 운용 리스크 중심 재분류

#### P0 — 즉시 (현재 운용 직접 영향)

| # | 내용 | 파일 | 수정 |
|:-:|:-----|:-----|:------|
| #6 | 매수 후 `held_quantities` + `sector_counts` 갱신 | `orchestrator.py` | 사이클 내 검사 정확도 확보 |
| #4 | `_refine_exit` 분봉 캐시 우선 사용 | `orchestrator.py` | KIS Rate Limit 절약 |
| #10 | Idempotency 키 사이클 단위 초기화 | `executor.py` | 재매수 차단 해소 |

#### P1 — 오늘 (시스템 안정성)

| # | 내용 | 파일 | 수정 |
|:-:|:-----|:-----|:------|
| #9 | `daily_loss_pct` None 체크 (`or`→`is not None`) | `risk.py` | 잘못된 리스크 중단 방지 |
| #5 | screener config 전달값 사용 (`_StrategyConfig()` 신규생성 제거) | `screener.py` | 동적 설정 반영 |
| #3 | `data_provider` NameError 방지 (`nonlocal` + None 체크) | `main.py` | 종료 크래시 방지 |

#### P2 — 이번주 (리스크 완화)

| # | 내용 | 파일 | 수정 |
|:-:|:-----|:-----|:------|
| #13 | 상관관계 검사 1개 포지션도 검사 | `orchestrator.py` | |
| #11 | 레짐 가중치 정규화 방어 | `regime.py` | |
| #17 | 미체결 취소 `microsecond=0` | `orchestrator.py` | |
| #18 | 패턴 탐지 예외 로깅 | `screener.py` | |
| #15 | ATR vol floor 주석 명확화 | `orchestrator.py` | |
| #16 | `_STATIC_NAMES` 비표준 코드 제거 | `real_data_provider.py` | |

#### P3 — 중장기 (백테스트 포함)

| # | 내용 | 파일 | 수정 |
|:-:|:-----|:-----|:------|
| #2 | `backtest.data_loader` import 제거 (자체 합성지수 생성) | `real_data_provider.py` | |
| #1 | 백테스트 `abs()` → `daily_loss_pct <= -limit` | `backtest/engine.py` | |
| #7 | 백테스트 market 코드 첫자리 기반 추론 | `backtest/engine.py` | |
| #8 | auto_retrain scaler 저장 확인 | `trainer.py` | |
| #12 | 백테스트 slippage floor (`rng_seed=0` 방지) | `backtest/engine.py` | |

---

## 4. 변경 파일 통계

| 파일 | P-51 | P-52 | P-53 | P-54 P0 | P-54 P1 | P-54 P2 | P-54 P3 |
|:-----|:----:|:----:|:----:|:-------:|:-------:|:-------:|:-------:|
| `data/universe_manager.py` | ✅ | ✅ | | | | | |
| `data/real_data_provider.py` | | | | | | ✅ | ✅ |
| `data/adapters/kis/adapter.py` | | | ✅ | | | | |
| `main.py` | ✅ | | ✅ | | ✅ | | |
| `live/orchestrator.py` | | | ✅ | ✅ | | ✅ | |
| `live/executor.py` | | | ✅ | ✅ | | | |
| `live/risk.py` | | | | | ✅ | | |
| `strategy/screener.py` | | | | | ✅ | ✅ | |
| `strategy/regime.py` | | | | | | ✅ | |
| `ml/training/trainer.py` | | | | | | | ✅ |
| `backtest/engine.py` | | | | | | | ✅ |
| 문서 (docs/) | 51 | 52 | 53 | 54 | — | — | — |

---

## 5. 잔여 이슈

| 구분 | 내용 | 상태 |
|:-----|:------|:------|
| ML 모델 | 분봉 단타 모델 AUC 0.689 → 실데이터 확장 학습 필요 | 미시작 |
| 문서화 | 본 문서 (`docs/55-2026-07-06-작업요약.md`) | ✅ |

---

*작성: 2026-07-06 21:00 KST*
