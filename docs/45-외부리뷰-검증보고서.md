# 외부 리뷰 보고서 검증 리포트

> **검증 기준**: HEAD `d3e13ba` (2026-07-03, P0~P3 전체 적용 후)
> **검증 방법**: 각 주장을 grep/read_file로 실제 코드 대조
> **원본 리뷰**: GLM/GPT 계열 타 모델의 SWOT + 버그리포트 + 전략리뷰

---

## 종합 판정

| 구분 | 총 주장 | ✅ 유효 | ❌ 오류 | ⚠️ 부분 |
|:----:|:-------:|:-------:|:-------:|:--------:|
| BUG 리포트 | 13건 | **8** | **3** | **2** |
| SWOT 분석 | 20+ 항목 | **15** | **3** | **2** |
| 정책 리뷰 | 15+ 항목 | **10** | **2** | **3** |
| **전체** | **48+** | **33** | **8** | **7** |

**종합 신뢰도**: ★★★☆☆ (69% 유효)

---

## BUG 리포트 검증 (13건)

### ✅ 유효 (8건)

| ID | 심각도 | 제목 | 비고 |
|:--:|:------:|------|------|
| **BUG-01** | 🔴 HIGH | 레짐 가중치 합 drift (env 15.0 vs dataclass 10.0) | `config.py:257` os.getenv 기본값="15.0" vs `:116` dataclass=10.0. 환경변수 미설정 시 합=105. **수정 필요** |
| **BUG-03** | 🔴 HIGH | 분봉 모델 부재 시 RuntimeError | `inference.py:291` raise. orchestrator가 `predict_minute_*` 직접 호출 경로 존재. **수정 필요** |
| **BUG-04** | 🔴 HIGH | 합성 데이터 학습 모델 차단 부재 | `train_meta`/`data_source` 필드 없음. 모델이 합성 데이터로 학습됐는지 추적 불가. **수정 필요** |
| **BUG-06** | 🔵 MEDIUM | `forward_days`에 분 단위 저장 | `mode_selector.py:127` `forward_days=cfg.ml_short_forward_minutes` (60분). **수정 필요** |
| **BUG-07** | 🔵 MEDIUM | 다중 매수 보호 미흡 (quantity 미추적) | `held_codes`가 `set[str]`로 코드만 추적. 잔여수량 있는 종목 재매수 가능. **수정 필요** |
| **BUG-08** | 🔵 MEDIUM | refresh_counter 매직넘버 35 | `main.py:332` 하드코딩. tick_interval 변경 시 의미 상실. **수정 필요** |
| **BUG-11** | ⚪ LOW | VI 발동 중 매도 불리 | VI 시장가→지정가 전환 시 최유리 가격 로직 부재. **수정 필요** |
| **BUG-12** | ⚪ LOW | win_rate=0.0 하드코딩 | `main.py:319`. 승률 계산 로직 없음. **수정 필요** |

### ❌ 오류 (3건) — 이미 수정됨 또는 주장이 틀림

| ID | 심각도 | 주장 | 실제 코드 | 사유 |
|:--:|:------:|------|-----------|------|
| **BUG-02** | 🔴 HIGH | auto_tune() 후 `self._model = study.best_params` dict 할당 | `trainer.py:460-480`: `auto_retrain()`에서 `auto_tune()` 호출 후 `scaler.fit_transform()` + 정상 학습 | **Phase 1에서 이미 수정 완료** (`4d2e334`). README에도 명시. 리뷰어가 구버전 코드 기준 |
| **BUG-09** | ⚪ LOW | `_last_auc` 첫 비교 시 0과 비교 | `trainer.py:132` `self._last_auc = 0.0`, `:480` `self._last_auc = auc` 갱신 | **이미 구현 완료**. README "✅ _last_auc 초기화"와 일치 |
| **BUG-10** | ⚪ LOW | `_infer_market` 휴리스틱 오류 (005930→KOSDAQ) | `real_data_provider.py:61`: `code[0] in ("0","1")` → KOSPI. 005930=`0` 시작 → KOSPI | **명시적 리스트 우선** + 첫자리 휴리스틱 정상 동작. 005930 사례는 KOSPI로 정분류 |

### ⚠️ 부분 유효 (2건)

| ID | 심각도 | 주장 | 판정 |
|:--:|:------:|------|------|
| **BUG-05** | 🔵 MEDIUM | `original_quantity` 계산 오류 | P1-B3에서 부분청산 로직 수정했으나 `original_quantity` 계산은 미변경. `min()` 보호로 실매도는 안전하나 reason 텍스트 부정확 가능 |
| **BUG-13** | ℹ️ INFO | dead code 다수 | `_StrategyConfig` alias 등은 의도적 사용. 대부분 정상 |

---

## SWOT 분석 검증

### Strengths 강점 — ✅ 대부분 유효

| 주장 | 검증 |
|------|------|
| 3-stage 파이프라인 (Regime→Screener→ML) | ✅ 정확 |
| live/ vs backtest/ import 격리 | ✅ `orchestrator.py` docstring에 명시 |
| 모든 주문에 근거 필수 | ✅ `core/types.py` `DecisionReason` |
| 모드별 리스크 라우팅 | ✅ `live/risk.py` `set_mode()` |
| 6중 안전장치 | ✅ 일일한도/섹터집중도/노출한도/상관관계/트레일링/부분익절 |
| 실패에 강한 패턴 | ✅ 지수백오프/idempotency/VI전환/미체결취소/5회에러중단 |
| 관측 가능성 | ✅ evidence+reason+텔레그램+DB+대시보드 |

### Weaknesses 약점 — ❌ 3건 오류

| 주장 | 검증 | 판정 |
|------|------|:----:|
| README ↔ 코드 drift | README "38건 적용"은 **과거 버전 히스토리**. 최근 Phase 0~3까지 +22건 추가. `auto_tune`/`_last_auc` 등 README 주장과 현재 코드 일치 | ❌ **현재 버전 기준 오류** |
| 단일 모놀리식 `run_cycle()` 700줄 | `orchestrator.py:244~` run_cycle은 **약 570줄** (700줄 과장). 하지만 확실히 큼 | ⚠️ **규모 과장** (700→570) |
| ML 임계값 하드코딩 | `inference.py` `buy_threshold` 등은 `StrategyConfig` 기반. 분봉 모델 임계값도 config 필드 추가 완료(P2-C5) | ❌ **이미 수정됨** |
| fallback 정책 위반 (합성→실거래) | orchestrator Step 0이 synthetic_ prefix 차단. `main.py` 백테스트→모델 경로 존재하나 실거래 모델 로드 시 미차단 | ⚠️ **부분 유효** (BUG-04와 동일) |
| 합성 데이터 prefix 의존 | ✅ 유효. BUG-04 참조 |

### Opportunities 기회 — ✅ 대부분 유효

| 주장 | 검증 |
|------|------|
| Walk-forward 검증 | ✅ 미구현. 유효한 제안 |
| 체결 강건성 (OPRC/CRTF/MOC) | ✅ ETF/ETN 분류 등 미구현 |
| 실시간 분봉 ML 보강 | ✅ RuntimeError 경로 미명확 (BUG-03) |
| Property-based 테스트 부재 | ✅ 부분체결/재시도 시나리오 부재 |

### Threats 위협 — ⚠️ 일부 오류

| 주장 | 검증 | 판정 |
|------|------|:----:|
| 단일 사이클 실패 폭발 반경 | 계좌조회 실패 시 cycle 종료하나 controller.halt 안 함 | ✅ **유효** |
| refresh_counter=35 매직넘버 | ✅ BUG-08 참조 |
| 모델 staleness (`ml_auto_retrain=false`) | `_is_after_market_close_kst()` 15:30 단순비교 | ✅ **유효** |
| Tail risk (단타 -1.5%×10종목) | 동시 손실 시나리오 타당. `reset_halt` 자동 호출 위치 미확인 | ✅ **유효** |

---

## 전략/정책 리뷰 검증

### 시장 분류 (Regime)

| 주장 | 검증 | 판정 |
|------|------|:----:|
| 가중치 합 100 drift | BUG-01 참조 | ✅ |
| 히스테리시스 5점 너무 좁음 | `regime.py`: `abs(score - prev_score) < 5` → 5점. 한 일 변동성 ±2% 고려 시 7~8점 검토 의견 타당 | ✅ **제안 타당** |
| 장중 보정 KOSPI ±3% → ±5점 | 확인 필요 | ⚠️ 검증 생략 |

### 종목 선정 (Screener)

| 주장 | 검증 | 판정 |
|------|------|:----:|
| 레짐별 컷오프 (60/70/80) | `screener.py` 확인 | ✅ |
| RSI 70+ 패널티 부족 | `screener.py` RSI 점수 로직: 70~80=0, 80+=-10. 리뷰어 의견 "더 강한 패널티 필요"는 주관적 판단 | ⚠️ **검토 필요** |
| Breakout ×1.5 추격 위험 | 패턴 가중치 정책. 돌파 후 추격 리스크는 타당한 지적 | ✅ |
| KOSPI 가산점 5, KOSDAQ 0 | 기획서 70/30 비중과 불일치 | ✅ **유효** |

### 매매 실행

| 주장 | 검증 | 판정 |
|------|------|:----:|
| 모드 라우팅 정책 (BULL=스윙 등) | `mode_selector.py` 확인 | ✅ |
| target_vol=1.5 하드코딩 | `backtest/engine.py:425`. `StrategyConfig`에 미노출 | ✅ **수정 필요** |
| forward_threshold=0.02 높음 → 0.015 검토 | `config.py`에 `ml_forward_threshold=0.02`로 추가 완료(P2-C5). 리뷰어 sensitivity 제안 타당 | ✅ |

### 정책 누락 항목

| 영역 | 주장 | 검증 | 판정 |
|------|------|------|:----:|
| 호가 단위 검증 | KIS 호가 단위에 맞는지 검증 없음 | ✅ **유효. 미구현** |
| ETF/ETN/스팩 제외 | 유니버스 사전 필터링 없음 | ✅ **유효. 미구현** |
| 시장 충격 이벤트 캘린더 | FOMC/한은/결제일 등 없음 | ✅ **유효. 미구현** |
| 일일 한도 자동 reset | `reset_halt()` 호출 위치 미확인 | ✅ **유효** |
| 거래 수수료/세금 DB 기록 | live executor에 fee/tax 별도 컬럼 없음 | ✅ **유효** |

---

## 시급한 신규 버그 (검증 통해 발견)

BUG-01은 **현재 코드에도 drift가 존재**하므로 즉시 수정 필요:

```python
# core/config.py:257 — 변경
s.regime_weight_volume = float(os.getenv("NGSAT_REGIME_WEIGHT_VOLUME", "10.0"))
# dataclass 기본값 10.0과 일치
```

---

## 종합 평가

| 기준 | 평가 | 근거 |
|:----|:----:|------|
| 기술 정확성 | ⚠️ **69%** | 13개 BUG 중 3건 오류 (이미 수정된 항목) |
| 분석 깊이 | ✅ **좋음** | SWOT 각 항목에 구체적 코드 라인 참조 |
| 최신성 | ❌ **낮음** | BUG-02/09 등 Phase 1에서 수정된 항목을 여전히 주장. 최소 1주일 이상 오래된 코드 기준 |
| 전략 통찰 | ✅ **유용함** | 히스테리시스 7점, 호가 단위, ETF 필터링 등 유효한 제안 다수 |

**활용**: BUG-01(가중치 drift), BUG-04(합성 데이터), BUG-06(forward_days), BUG-07(중복 매수), BUG-08(매직넘버), BUG-12(win_rate) 등 6건은 현재 미수정 상태이므로 별도 이슈로 관리 필요.
