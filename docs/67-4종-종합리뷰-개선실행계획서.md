# 67-4종-종합리뷰-개선실행계획서

> **작성일**: 2026-07-07  
> **기준 HEAD**: 7e6556d  
> **근거 리뷰**: (1) 백엔드 코드 리뷰 (2) 대시보드 UI/UX 리뷰 (3) 전략/퀀트 리뷰 (4) 종합 리포트  
> **총 발견 건수**: 43건 (Critical 5, High 12, Medium 16, Low 10)

---

## 1. 리스크 검토 총칙

각 수정 작업은 다음 리스크 범주로 사전 평가합니다:

| 등급 | 정의 | 수정 전 필수 조치 |
|:----:|------|-------------------|
| 🔴 R-critical | 실거래 중단/자금 손실 가능 | 오프라인 검증 후 배포, 장마감 후 적용 |
| 🟡 R-high | 기능 오작동, 복구 1시간+ | staging 테스트 후 다음 장 적용 |
| 🔵 R-medium | 부차적 기능 영향, 복구 30분 | 코드리뷰 필수, 장중 가능 |
| 🟢 R-low | UI/로깅/문서 변경 | 즉시 적용 가능 |

---

## 2. Phase 0: 즉시 조치 (장마감 후 → 내일 장전)

리스크 최소화를 위해 **장 운영에 직접 영향을 주지 않는 범위**에서 우선 처리.

### PA-1: 일일 손실 한도 부호 수정 🔴 R-critical

| 항목 | 내용 |
|------|------|
| **대상** | `live/risk.py:151` `check_daily_loss()` |
| **현황** | `loss_pct >= limit_pct` → KIS 음수 수익률에서 항상 False |
| **수정안** | **A안 (권장):** `if abs(loss_pct) >= limit_pct` — KIS 부호 무관 안전 |
| | **B안:** `if loss_pct <= -limit_pct` — 백테스트와 일관성 |
| | **C안:** 조건문 전 `loss_pct = abs(loss_pct)` 변환 — 최소 변경 |
| **리스크 검토** | ⚠️ KIS `daily_loss_pct`가 이미 양수(절대값)로 오는 경우 `abs()`가 정상 동작을 무력화. **반드시 KIS 실제 응답 포맷 확인 후 적용** |
| **확인 방법** | 서버 로그 또는 KIS API 테스트로 응답 부호 확인. `test_live/test_risk.py`에 해당 테스트 추가 |
| **실행 조건** | KIS 응답 포맷 확인 완료 후 → 장마감 직후 적용 |
| **예상 공수** | 15분 (확인) + 10분 (수정+테스트) |
| **의존성** | 없음 |

### PA-2: ATR min_pct=base*1.0 → 0.3 수정 🔴 R-critical

| 항목 | 내용 |
|------|------|
| **대상** | `live/orchestrator.py:568` |
| **현황** | `min_pct = base_budget_pct * 1.0` → ATR 축소 불가 |
| **수정안** | `min_pct = base_budget_pct * 0.3` 으로 변경 |
| **리스크 검토** | ⚠️ 포지션 사이즈가 현재보다 70% 축소될 수 있음. **계좌가 200만 원인 현재는 1주 단위 양자화로 차이 없을 가능성 높음** → 리스크 낮음 |
| | ⚠️ 단타모드에서도 동일 적용 → `mode_short_position_size=0.05`와 곱해져 1.5%까지 축소 가능 |
| **추가 개선** | `calc_position_size()`를 모든 모드에서 사용하도록 통일 (현재 mode="hold"에서만 호출) — Phase 1으로 분리 |
| **실행 조건** | 장마감 후 적용 |
| **예상 공수** | 10분 |
| **의견** | **대표님, P0-1과 P0-2는 실거래 리스크가 있는 코드라 확인 후 오늘 장마감 직후 수정하겠습니다. 세 가지 모두 단순 numerical fix라 회귀 리스크가 낮습니다.** |

### PA-3: 제어 동작 에러 Toast 추가 🔴 R-critical, 🟢 R-low

| 항목 | 내용 |
|------|------|
| **대상** | `App.jsx:75-85` `handleControl()` |
| **현황** | `api.start()/stop()/forceHold()` 결과 무시 |
| **수정안** | try/catch 후 실패 시 `addToast({ type: 'error', message: '매매 시작 실패: ...' })` |
| **리스크 검토** | 🟢 프론트엔드 only, 백엔드 영향 없음. 에러 토스트가 없는 것보다 있는 게 무조건 안전 |
| **실행 조건** | 즉시 가능 |
| **예상 공수** | 15분 |

---

## 3. Phase 1-1: 당일 장중 진행 가능 (🟢 R-low ~ 🔵 R-medium)

### PB-1: 전탭 5초 전량 폴링 → 탭 기반 선택적 갱신 🔴 R-critical → 🔵 R-medium

| 항목 | 내용 |
|------|------|
| **대상** | `App.jsx:69-73` `refreshAll()` |
| **현황** | 5초마다 7개 API 전부 호출 (84회/분) |
| **수정안** | **A안 (권장):** `activeTab` 상태 기반 선택적 fetch — 운영탭에서는 7개, 진단탭에서는 diagnosis만 |
| | **B안:** Polling 주기를 탭별 차등 (운영 3초, 설정 10초) |
| | **C안:** WebSocket 전환 (Phase 2로 분리) |
| **리스크 검토** | 🟢 탭 기반 fetch는 현재 `activeTab`이 App.jsx에 이미 존재(`useState('operations')`), 조건부 fetch만 추가. API 응답 구조 변경 없음 |
| **실행 조건** | 즉시 가능 |
| **예상 공수** | 1시간 |

### PB-2: ML auto_retrain=True + auto_select_model=True 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `core/config.py` `ml_auto_retrain`, `ml_auto_select_model` |
| **현황** | 둘 다 False. AUC 68.9% 정체 |
| **수정안** | 기본값 True 전환. 단, 재학습 주기(`ml_retrain_interval_days=7`) 유지 |
| **리스크 검토** | ⚠️ **재학습 시 CPU/메모리 부하** — 장마감 후 학습 전환 확인 필요. 장중 재학습 시 590줄 `trainer.py`의 `train_model()`이 10초 이상 점유 가능 |
| | ⚠️ **모델 성능이 갑자기 하락할 경우 fallback** — 이전 모델 보관 로직 확인 필요 |
| | **해결**: `retrain_hour=15:30`으로 장마감 직후로 제한. `persistence.py`에 이전 모델 3세대 보관 확인 |
| **실행 조건** | 장마감 후 첫 재학습 모니터링 후 → 다음날 자동 전환 |
| **예상 공수** | 30분 (설정 변경) + 1일 (모니터링) |

### PB-3: Trailing Stop 활성화 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `core/config.py:163` `trailing_stop_enabled = True` |
| **현황** | 코드 구현 완료, 기본 False |
| **수정안** | `trailing_stop_enabled = True`, `atr_multiplier=2.0`, `activate_pct=1.0` |
| **리스크 검토** | ⚠️ **기존 포지션에 소급 적용 여부** — 이미 진입한 포지션에 trailing stop이 적용되면 갑작스러운 청산 발생. 신규 포지션만 적용해야 함. `executor.py`의 `submit_sell()` 경로 확인 필요 |
| | ATR multiplier 2.0은 합리적. ATR이 0인 안정 장에서는 활성화되지 않음 |
| **실행 조건** | 신규 포지션만 적용 확인 후 전환 |
| **예상 공수** | 30분 (코드 확인) + 10분 (설정 변경) |

### PB-4: 백테스트 슬리피지 0.1%→0.2% 상향 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `backtest/engine.py:162-188` |
| **현황** | 0.1% (normal) / 0.3% (urgent) |
| **수정안** | 0.2% (normal) / 0.5% (urgent) + 저유동성 종목은 0.5% 기본 적용 |
| **리스크 검토** | 🟢 백테스트만 영향. 실거래 영향 없음. 단, 기존 백테스트 결과와 단순 비교 불가 → 보고서에 변경 사항 명시 |
| **실행 조건** | 즉시 가능 |
| **예상 공수** | 20분 |

### PB-5: 연결 상태 시각화 개선 🟢 R-low

| 항목 | 내용 |
|------|------|
| **대상** | `App.jsx:165-168` |
| **현황** | 8px 점 + "연결됨"/"미연결" 텍스트만 |
| **수정안** | 연결 끊김 시 상단 빨간 배너 `bg-red-600 text-white px-4 py-2 text-center fixed top-0 w-full z-50` |
| **리스크 검토** | 🟢 프론트엔드 only, CSS overlay |
| **예상 공수** | 30분 |

---

## 4. Phase 1-2: 금주 완료 (백엔드 위주)

### PC-1: Rate Limiter 중앙 집중화 (KisRateLimiter) 🔴 R-critical → 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `data/adapters/kis/client.py:79` token bucket 확장 |
| **수정안** | 전역 `KisRateLimiter` 클래스: asyncio.Semaphore(5) + TokenBucket(rate=20, burst=30) |
| | 적용 범위: adapter.py 전 API + websocket_client.py |
| | 기존 `asyncio.sleep(0.05)` 등 15+군데 sleep 제거 |
| **리스크 검토** | ⚠️ **가장 위험한 수정** — 모든 KIS API 호출이 rate limiter를 통과해야 하므로, limiter에 버그가 있으면 **전체 거래 중단** |
| | **반드시**: (1) 단위 테스트 작성 (2) staging에서 1일 모니터링 (3) kill switch: limiter 우회 플래그 |
| | **추가**: Rate Limit 초과 시 fallback → 짧은 cooldown 후 재시도 (현재는 그냥 sleep) |
| **의존성** | 없음 (독립 모듈) |
| **예상 공수** | 4-6시간 (설계+구현+테스트) |
| **권장** | **별도 PR로 분리, 충분한 테스트 후 주말 배포 권장** |

### PC-2: 상관관계 임계 0.85→0.75 + 가중 검사 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `live/orchestrator.py:183-236` `_check_correlation()` |
| **현황** | threshold=0.85, avg_corr > 0.5 검사 |
| **수정안** | (1) threshold 0.85→0.75 (2) 단순 count→가중 합계 (3) 최근 구간 가중치 증가 |
| **리스크 검토** | ⚠️ 상관관계가 높은 포지션 쌍이 더 많이 차단됨 → 포트폴리오 다양성 증가 but 거래 기회 감소. 0.75도 높은 수준이므로 영향 제한적 |
| **예상 공수** | 1시간 |

### PC-3: can_extend_stop_loss() 호출 연결 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `live/risk.py:206` → `live/orchestrator.py` |
| **현황** | 함수 정의만 있고 호출 없음 |
| **수정안** | `run_cycle()` 청산 단계에서 체결 후 `can_extend_stop_loss(position, regime_score)` 호출하여 손절선 연장 |
| **리스크 검토** | 🔵 하락장에서 손절선 확장 = 더 긴 손실 허용. 리스크 증가. 반드시 regime_score가 낮을 때만 확장하도록 가드 필요 |
| **예상 공수** | 1시간 |

### PC-4: 중립장 min_score 30→35 재조정 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `core/config.py:122` `screener_neutral_min_score = 30` |
| **수정안** | 35 또는 40으로 상향. 단, P-60에서 하락장 대응으로 30으로 낮춘 결정이므로 **5월~6월 백테스트로 검증 후 결정** |
| **리스크 검토** | 🟢 min_score만 변경. max_candidates=10 제한이 이미 있으므로 상위권 종목에 영향 없음 |
| **의존성** | 백테스트 검증 필요 |
| **예상 공수** | 30분 (변경) + 2시간 (백테스트 검증) |

---

## 5. Phase 2: 다음주 (구조 개선)

### PD-1: Orchestrator 분할 설계 🔴 R-critical (SRP)

| 항목 | 내용 |
|------|------|
| **대상** | `live/orchestrator.py` 1110줄 |
| **제안 분할** | |
| | **1) `live/cycle_scheduler.py` (150줄)** — tick 관리, 타이밍, cycle_count |
| | **2) `live/trade_pipeline.py` (400줄)** — run_cycle() 단계별 실행: 레짐→스크리닝→ML→청산→진입 |
| | **3) `live/position_manager.py` (200줄)** — 포지션 추적, P&L, 미체결 정리 |
| | **4) 기존 파일 유지:** `orchestrator.py`는 300줄로 축소 (파이프라인 오케스트레이션만) |
| **리스크 검토** | ⚠️ **가장 큰 변경**. 현재 1110줄에 14개 `except Exception`이 분산되어 있어 리팩토링 중 로직 변경 위험 |
| | **대응**: (1) 기존 테스트 329개 전부 통과 확인 (2) 각 분할 단계별 별도 PR (3) 분할 후에도 동일한 동작 보장하는 Snapshot 테스트 |
| | **추천**: 1주일 작업 + 3일 통합 테스트 |
| **예상 공수** | 2-3주 |
| **선행 조건** | Rate Limiter(PC-1) 완료 → API 호출 패턴 안정화 후 진행 |

### PD-2: DB Repository 패턴 통일 🔴 R-critical

| 항목 | 내용 |
|------|------|
| **대상** | `live/orchestrator.py:147-150` raw session → `TradeRepository`로 통일 |
| **수정안** | orchestrator 내 `self._Session()` 호출 제거, 모든 DB 접근을 `self._trade_repo`로 대체 |
| **리스크 검토** | ⚠️ orchestrator의 DB 접근 10+군데를 일괄 변경. 트랜잭션 경계 변경으로 데이터 무결성 영향 가능 |
| | **대응**: 변경 전 라인별 DB 접근 목록 작성 → 하나씩 교체하며 테스트 통과 확인 |
| **예상 공수** | 1-2주 |
| **선행 조건** | Orchestrator 분할(PD-1)과 병행 또는 순차 가능 |

### PD-3: FastAPI lifespan 패턴 + shutdown 안정화 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `main.py` L655-661 (수동 shutdown) |
| **수정안** | FastAPI `lifespan` 컨텍스트 매니저로 startup/shutdown 공식 지원 |
| | WebSocket task를 `tasks` 리스트에 등록 + cancel 순서 보장 |
| **리스크 검토** | 🔵 main.py startup 시퀀스 변경 → 초기화 실패 시 서비스 불가. 기존 startup 로직 그대로 유지하면서 lifespan 래퍼만 추가 |
| **예상 공수** | 1일 |
| **선행 조건** | 없음 |

### PD-4: API 계층 DTO/인터페이스 분리 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `dashboard/backend/api.py` (10+군데 orchestrator `_` 속성 직접 접근) |
| **수정안** | 안전한 public getter 메서드 또는 DTO 클래스 도입 |
| | `orch.get_status()`, `orch.get_positions()`, `orch.get_diagnosis()` 등 |
| **리스크 검토** | 🟢 기존 getattr 패턴을 새 메서드로 대체. API 응답 구조 불변 |
| **예상 공수** | 1주 |
| **선행 조건** | Orchestrator 분할(PD-1) 완료 후 — 분할되면 인터페이스 설계가 더 명확해짐 |

### PD-5: 코드 스플리팅 (React.lazy) 🟢 R-low

| 항목 | 내용 |
|------|------|
| **대상** | `main.jsx` |
| **수정안** | BacktestPanel, DiagnosisPanel, StrategyConfigPanel을 `React.lazy(() => import(...))` + Suspense |
| **리스크 검토** | 🟢 Vite가 code splitting 기본 지원. 초기 번들 크기만 감소 |
| **예상 공수** | 30분 |

---

## 6. Phase 3: 중장기

### PE-1: datetime.now()→KST 표준화 🔵 R-medium

| 항목 | 내용 |
|------|------|
| **대상** | 전역 `datetime.now()` 사용처 5+군데 |
| **수정안** | `core/types.py`의 `KST` timezone 사용 통일. `datetime.now(KST)` |
| **리스크 검토** | 🟢 단순 표준화. 시간 비교 로직(`cancel_unfilled_orders`)의 자정 넘김 처리만 추가 확인 |
| **예상 공수** | 2시간 |

### PE-2: WebSocket URL 환경변수화 + wss:// 검토 🔵 R-medium

| 항목 | 내용 |
|------|------|
| **대상** | `websocket_client.py:31` |
| **수정안** | `config.kis.ws_url`로 이동. KIS wss:// 지원 여부 확인 후 전환 |
| **예상 공수** | 1시간 |

### PE-3: 백테스트 abs() #1 + import 위반 #2 수정 🔵 R-medium

| 항목 | 내용 |
|------|------|
| **대상** | `backtest/*` (P3로 분류, 미수정) |
| **리스크 검토** | 🟢 백테스트 only. 실거래 무관. 이미 버그로 분류된 사항 |
| **예상 공수** | 2-4시간 |

### PE-4: entry/exit_timing 파라미터 config 이동 🟢 R-low

| 항목 | 내용 |
|------|------|
| **대상** | `strategy/entry_timing.py:49`, `exit_timing.py:54` 하드코딩 상수 |
| **수정안** | StrategyConfig에서 주입 → 대시보드에서 실시간 조정 가능 |
| **예상 공수** | 3시간 |

### PE-5: 통합 테스트 추가 🟢 R-low

| 항목 | 내용 |
|------|------|
| **대상** | `tests/` (현재 27개 단위테스트 파일, 1개 통합테스트) |
| **수정안** | (1) Rate Limit 동작 테스트 (2) Shutdown 시퀀스 테스트 (3) 동시성(race condition) 테스트 |
| | (4) KIS Mock 응답 기반 엔드투엔드 시나리오 3~5개 |
| **예상 공수** | 1주 |
| **선행 조건** | Rate Limiter(PC-1) 완료 후 |

### PE-6: 장중보정 KOSDAQ 지수 추가 🔵 R-medium

| 항목 | 내용 |
|------|------|
| **대상** | `live/orchestrator.py:364` |
| **수정안** | KOSPI + KOSDAQ 지수 평균 또는 max(등락률) 사용 |
| **리스크 검토** | 🔵 장중보정 계수 변경 → 레짐 재평가 결과에 영향. hysteresis 범위 내에서 안정적일 가능성 높음 |
| **예상 공수** | 1시간 |

### PE-7: 모드 전환 Grace Period 🔵 R-medium

| 항목 | 내용 |
|------|------|
| **대상** | `live/orchestrator.py:401` |
| **수정안** | 모드 전환 시 `mode_changed_at` 저장 → 기존 포지션은 이전 손절선 유지, 신규 진입만 새 파라미터 |
| **예상 공수** | 2시간 |

### PE-8: Partial TP 활성화 검토 🟡 R-high

| 항목 | 내용 |
|------|------|
| **대상** | `core/config.py:168` `partial_tp_enabled` |
| **수정안** | True로 전환. 단, ATR 기반 TP(tp_atr_multiplier=2.0)가 유의미한 수준인지 백테스트 검증 필요 |
| **리스크 검토** | ⚠️ 조기 부분 익절로 전체 수익률이 낮아질 수 있음. 수익 변동성은 낮아짐. **반드시 백테스트로 검증 후 적용** |
| **예상 공수** | 1일 (검증) + 10분 (설정) |

---

## 7. 실행 일정 종합

```
Phase 0: 오늘 장마감 후 (7/7)
├── PA-1: risk.py 부호 수정 (★ P0-1)
├── PA-2: ATR min_pct 1.0→0.3 (★ P0-2)
├── PA-3: 제어 에러 Toast (★ P0-5)

Phase 1-1: 당일~내일 (7/7~7/8)
├── PB-1: 탭 기반 폴링 (★ P0-4)
├── PB-2: ML auto_retrain=True (P1-1)
├── PB-3: Trailing Stop 활성화 (P1-2)
├── PB-4: 슬리피지 0.2% 상향 (P1-4)
├── PB-5: 연결 상태 배너 (P1-5)

Phase 1-2: 금주 (7/8~7/11)
├── PC-1: Rate Limiter (★ P0-3) — 주말 배포 권장
├── PC-2: 상관관계 0.85→0.75
├── PC-3: can_extend_stop_loss() 연결
├── PC-4: 중립장 min_score 35~40

Phase 2: 다음주 (7/14~)
├── PD-1: Orchestrator 분할 (2-3주)
├── PD-2: Repository 패턴 통일
├── PD-3: FastAPI lifespan + shutdown
├── PD-4: API DTO 분리
├── PD-5: 코드 스플리팅

Phase 3: 중장기
├── PE-1~PE-8 (datetime/WS URL/백테스트버그/config 이동/통합테스트/KOSDAQ/Grace/PartialTP)
```

---

## 8. Change Impact Matrix

| 파일 | 변경 횟수 | Phase | 영향범위 | 회귀 리스크 |
|------|:---------:|:-----:|:---------:|:-----------:|
| `live/orchestrator.py` | 4 | 0,1,2 | 매매 전체 | 🔴 높음 |
| `live/risk.py` | 1 | 0 | 리스크 관리 | 🔴 높음 |
| `core/config.py` | 7 | 1,2,3 | 전역 설정 | 🟡 중간 |
| `App.jsx` | 3 | 0,1 | 프론트 | 🟢 낮음 |
| `dashboard/backend/api.py` | 2 | 2 | API 응답 | 🟡 중간 |
| `backtest/engine.py` | 1 | 1 | 백테스트 | 🟢 낮음 |
| `data/adapters/kis/` | 2 | 1,2 | KIS 통신 | 🔴 높음 |
| `ml/*` | 2 | 1 | ML 학습 | 🟡 중간 |
| `main.py` | 1 | 2 | 서버 생명주기 | 🔴 높음 |

---

## 9. 권장 실행 전략

### 대표님 의사결정 포인트

1. **P0-1 (risk 부호)**: KIS 응답 부호 확인 필요. 지금 서버 로그에서 `daily_loss_pct` 값을 확인하시겠습니까?

2. **Orchestrator 분할 타이밍**: 2-3주 작업. Rate Limiter 먼저(리스크) vs 분할 먼저(구조). 개인적으로는 **Rate Limiter를 먼저** 하는 것이 리스크 관리 측면에서 우선이라고 판단됩니다.

3. **Trailing Stop 활성화**: 수익 보호 vs 거래 기회 감소의 트레이드오프. 단타 모드(1.5% 손절)에서는 trailing stop이 큰 의미 없을 수 있습니다.

### 추천 실행 순서

```
1순위: KIS 응답 확인 → PA-1 + PA-2 (오늘 장마감)
2순위: PA-3 + PB-1 + PB-5 (장중 가능, 리스크 낮음)
3순위: PC-1 Rate Limiter (이번주 설계 + 주말 배포)
4순위: PB-2 + PB-3 + PB-4 (다음주 초)
5순위: PC-2 + PC-3 + PC-4 (다음주 중)
6순위: PD-1~PD-5 구조 개선 (다다주~)
```

---

*계획서 작성 완료. 모든 항목은 실제 코드 검증 기반이며, 리스크 등급은 각 변경이 실거래에 미치는 영향을 기준으로 산정하였습니다.*
