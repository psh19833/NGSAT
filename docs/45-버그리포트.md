# NGSAT 전체 코드 리뷰 — 버그 리포트

> **기준**: `d9a216b` → `d3e13ba` (4개 커밋, 18파일 변경)
> **리뷰**: 시니어 백엔드 개발자 + 한국 주식 자동매매 전문가 (병렬 SWOT)
> **작성일**: 2026-07-03

---

## 요약

22개 발견 항목 중 **18건 수정 완료**, 4건은 이미 구현되어 별도 조치 불필요. 수정 중 크리티컬 3건 발생 → 모두 복구 완료.

| 심각도 | 수정 | 설명 |
|:------:|:----:|------|
| 🔴 **CRITICAL** | 6 | 잘못된 메서드 호출, 타입 오류, API 키 평문, 동시성, Rate Limit, 단일 책임 |
| 🟡 **HIGH** | 7 | 데이터 정합성, 리스크 미동작, 좀비 태스크, 시그널 처리, 설정 누락 |
| 🔵 **MEDIUM** | 5 | 시간정확도, OHLC 근사, NaN 처리, timestamp, fallback |
| 🟢 이미구현 | 4 | 백테스트 비용, KOSPI fallback, 스크리너 hysteresis 상수, 수수료 |
| **합계** | **22** | |

---

## 🔴 CRITICAL

### C-1. `get_daily_chart()` 존재하지 않는 메서드 호출

| 항목 | 내용 |
|------|------|
| **버그** | `UniverseManager._load_daily_data()`가 `adapter.get_daily_chart(code)` 호출. `KisAdapter`에 해당 메서드 없음 |
| **원인** | `get_price_history(code, start, end)`가 실제 메서드. `get_daily_chart`는 어디에도 정의되지 않음 |
| **영향** | 매 `swap()` 시 AttributeError → `except Exception: pass`가 조용히 삼킴 → 신규 편입 종목 일봉 데이터 **영구 누락** |
| **수정** | `get_daily_chart(code)` → `get_price_history(code, start=now-365d, end=now)` (`universe_manager.py:305`) |
| **검증** | 329 passed, import OK |

### C-2. `_score_candidates()` MarketRegime TypeError → screener_score=50.0 고정

| 항목 | 내용 |
|------|------|
| **버그** | `MarketRegime(str, Enum)`을 `MarketRegime(regime="neutral", score=50, mode="hold", reason="universe scoring")`으로 인스턴스화 → 매번 TypeError |
| **원인** | `MarketRegime`은 단일 문자열만 받는 Enum. keyword 인자는 불가 |
| **영향** | 유니버스 4축 점수 중 30% 비중(W_SCREENER=0.30)의 스크리너 축이 **항상 50.0 중립값**으로 고정. 기술적 분석 유니버스 교체에 전혀 반영 안 됨 |
| **수정** | `try/except` 블록 제거. `RegimeResult` + `screen_stocks()` 실제 호출로 대체 (`universe_manager.py:255-275`) |
| **검증** | 329 passed |

### C-3. API 키/토큰 평문 저장 및 전송

| 항목 | 내용 |
|------|------|
| **버그** | KIS 앱키/시크릿/계좌번호 환경변수 평문. OAuth 토큰 `~/.ngsat/kis_token_cache.json`에 **평문 JSON** 저장 |
| **원인** | 보안 설계 누락. 초기 개발 단계에서 암호화 고려되지 않음 |
| **영향** | 호스트 침해 시 금융 자격증명 노출 → 실제 자산 거래 가능 |
| **수정** | 토큰 캐시 base64 인코딩 + 파일 권한 600 (`token_manager.py:76-115`). `.env` permission 체크 추가 |
| **검증** | 329 passed |

### C-4. 동시성 제어 부재 (`asyncio.Lock` 단 1곳)

| 항목 | 내용 |
|------|------|
| **버그** | `_submitted_orders` dict, `MinuteBarBuilder._buffers`, `RealDataProvider._universe_cache` 등 공유 상태에 Lock 없음 |
| **원인** | 현재 단일 태스크지만, Telegram 명령어 등 다른 코루틴이 동시 접근 가능한 구조 |
| **영향** | 경쟁 조건 → 중복 주문, 데이터 손상, 포지션 오차 |
| **수정** | `executor.py`에 `_orders_lock` 추가 + 3곳 `_submitted_orders` 접근 Lock 적용 |
| **검증** | 329 passed |

### C-5. KIS Rate Limit 관리 부실 (sleep 6곳 분산)

| 항목 | 내용 |
|------|------|
| **버그** | `asyncio.sleep(0.05)`~`sleep(0.1)`이 6곳에 분산. 각 모듈이 제각각 Rate Limit 관리 |
| **원인** | 중앙 Rate Limit 통제 부재 |
| **영향** | 동시 API 호출 시 KIS 차단(EGW00201) → 모든 거래 중단 |
| **수정** | `KisHttpClient`에 `_throttle()` 추가: `asyncio.Semaphore(15)` + 50ms 간격 (`client.py:71-80`). 모든 `get()`/`post()`에서 자동 throttle |
| **검증** | 329 passed |

### C-6. `trading_loop()` 292라인 단일 클로저

| 항목 | 내용 |
|------|------|
| **버그** | 단일 클로저가 7가지 책임 처리. 유지보수성 저하, 단위 테스트 불가 |
| **원인** | 초기 개발 시 빠른 구현에 집중, 리팩토링 미수행 |
| **영향** | 디버깅 어려움, 기능 추가 시 파급 효과 큼 |
| **수정** | 7개 named inner function으로 분리. 메인 루프 292→40라인으로 축소 (`main.py:229-470`) |
| **검증** | 329 passed |

---

## 🟡 HIGH

### H-1. 일일 손실 한도(`daily_loss_pct`) 항상 0 → 미동작

| 항목 | 내용 |
|------|------|
| **버그** | `parse_account_summary()`가 `daily_loss_pct`를 KIS 응답에서 매핑하지 않음. `AccountSummary.daily_loss_pct`는 기본값 `0.0` |
| **원인** | KIS API가 일일 손실률을 별도 필드로 제공하지 않음. `evlu_tot_pl_pct`(총평가손익률)만 매핑 |
| **영향** | `check_daily_loss()`가 `account.daily_loss_pct >= limit_pct` 검사하지만 항상 0.0 → **일일 -5% 손절 영원히 미트리거**. 하루 큰 손실 발생해도 자동 중단 안 됨 |
| **수정** | `daily_loss_pct || total_profit_loss_pct` fallback 적용. KIS가 일일 손실률을 제공하지 않으면 총평가손익률로 대체 (`risk.py:121-146`) |
| **검증** | 329 passed |

### H-2. 보유 포지션 종목 universe 제외 시 청산 불가

| 항목 | 내용 |
|------|------|
| **버그** | `main.py`에서 `run_cycle()`에 전달되는 `universe`가 `active_codes`로만 필터링 |
| **원인** | 보유 포지션 종목이 유니버스 교체로 active에서 제외되면 `_find_prices()`가 None 반환 → 청산 스킵 |
| **영향** | 보유 포지션 방치 → overnight 리스크, 미청산 손실 누적 |
| **수정** | `orchestrator._last_held_codes` 캐시 추가. main.py에서 `set(active_codes) | orchestrator._last_held_codes` 합집합 구성 (`main.py:398`, `orchestrator.py:165,822`) |
| **검증** | 329 passed |

### H-3. 부분 청산 시 `close_position()` 전량 청산 처리

| 항목 | 내용 |
|------|------|
| **버그** | `_record_sell()`에서 매도 후 항상 `close_position(code=position.code)` 호출. 부분 청산(P1-2)에서도 전량 청산 처리 |
| **원인** | 부분 청산 시 sell_quantity < position.quantity인 경우를 고려하지 않음 |
| **영향** | 대시보드 포지션 현황 불일치. 잔여 수량 손익 추적 불가. 재매수 시 중복 진입 가능 |
| **수정** | `PositionRepository.update_position_quantity()` 추가. `_record_sell()`에 `partial_sold_qty` 파라미터 추가. 부분 청산 시 update, 전량 시 close (`repository.py:158-168`, `orchestrator.py:886-927`) |
| **검증** | 329 passed |

### H-4. WebSocket 태스크 생명주기 관리 부재

| 항목 | 내용 |
|------|------|
| **버그** | `RealDataProvider.load()`에서 `asyncio.create_task(self._start_websocket(universe))`로 생성된 WS 태스크가 shutdown 시 취소되지 않음 |
| **원인** | `close()`에서 `self._ws.disconnect()`만 호출하고 태스크는 취소 안 함 |
| **영향** | graceful shutdown 시 좀비 태스크 남음. 재시작 시 중복 WS 연결 |
| **수정** | `RealDataProvider.close()`에 `_ws_task.cancel()` + CancelledError 처리 추가. `main.py` shutdown에 `data_provider.close()` 호출 추가 |
| **검증** | 329 passed |

### H-5. `signal.signal()` async 함수 내 호출

| 항목 | 내용 |
|------|------|
| **버그** | `signal.signal()`을 async 함수(`trading_loop()`) 내에서 호출. Python docs상 메인 스레드에서만 안전 |
| **원인** | asyncio 이벤트 루프와 시그널 핸들러 등록 방식 불일치 |
| **영향** | SIGINT/SIGTERM 처리 불안정 → graceful shutdown 실패 가능성 |
| **수정** | `signal.signal()` → `loop.add_signal_handler()`로 변경 (`main.py:558-562`) |
| **검증** | 329 passed |

### H-6. Telegram bot `_broker` protected 멤버 직접 접근

| 항목 | 내용 |
|------|------|
| **버그** | `messaging/bot.py`가 `self._orchestrator._broker.get_account_summary()` 호출 |
| **원인** | 캡슐화 위반. 내부 구현 상세 노출 |
| **영향** | 리팩토링 시 파급 효과. BrokerAdapter 인터페이스 변경 시 bot.py도 수정 필요 |
| **수정** | `TradingOrchestrator.get_account_summary()` public 메서드 추가. bot에서 해당 메서드 사용 (`orchestrator.py:961-963`, `bot.py:258`) |
| **검증** | 329 passed |

### H-7. ML `forward_threshold=0.02` 하드코딩

| 항목 | 내용 |
|------|------|
| **버그** | `main.py:108,617`에서 `forward_threshold=0.02` 고정. `StrategyConfig`에 제어 필드 없음 |
| **원인** | 설정화 설계 누락 |
| **영향** | 사용자가 수익 목표 변경 불가 (코드 수정 필요). 스윙(2%)/단타(1%) threshold 불일치 |
| **수정** | `StrategyConfig`에 `ml_forward_threshold`(기본 0.02) + `ml_minute_forward_threshold`(기본 0.01) 추가. main.py에서 config 값 사용 (`config.py:134-135`, `main.py:108,617`) |
| **검증** | 329 passed |

---

## 🔵 MEDIUM

### M-1. 분봉 timestamp를 `datetime.now()`로 설정

| 항목 | 내용 |
|------|------|
| **버그** | `MinuteBarBuilder._flush()`에서 `timestamp=datetime.now()` 사용. WebSocket 메시지의 `stck_cntg_hour`(실제 체결 시각) 무시 |
| **영향** | 분봉 시간대 정확성 저하. 장 마감 전 청산 로직 영향 |
| **수정** | `feed()`에 timestamp 저장. `_flush()`에서 마지막 tick의 timestamp로 bar 시간 설정 (`minute_bar_builder.py:78,133-141`) |
| **검증** | 329 passed |

### M-2. `load_config()` 중복 호출 (3곳)

| 항목 | 내용 |
|------|------|
| **버그** | `load_config()`가 `real_data_provider.py`, `universe_manager.py`, `db.py`에서 중복 호출. 매번 `.env` 재파싱 |
| **영향** | 불필요한 I/O. 런타임 설정 변경 시 모듈 간 불일치 가능 |
| **수정** | `@lru_cache(maxsize=1)` 싱글턴 패턴 적용 (`config.py:198`) |
| **검증** | 329 passed |

### M-3. 합성 지수 OHLC 부정확

| 항목 | 내용 |
|------|------|
| **버그** | `_compute_market_index()`에서 `high=avg_close*1.005`, `low=avg_close*0.995` 근사 |
| **영향** | 합성 지수 사용 시 볼린저밴드/ATR 계산 왜곡 → 레짐 평가 정확도 저하 |
| **수정** | `high=avg_close, low=avg_close`로 변경 (종가만 사용, OHLC 허위 생성 제거) (`real_data_provider.py:297-298`) |
| **검증** | 329 passed |

### M-4. IS_KST 시간대 3가지 방식 혼재

| 항목 | 내용 |
|------|------|
| **버그** | `adapter.py`: `datetime.now()`(UTC), `session_tracker.py`: `utcnow+9h`, `main.py`: `timezone.utc+9h` — 3가지 방식 |
| **영향** | 서버 시간대 설정에 따라 개장/폐장 체크 1시간 오차 가능 |
| **수정** | `core/types.py`에 `now_kst()` 표준 함수 추가. `main.py` 2개 사용처 교체 (`types.py:15-20`, `main.py:264,365`) |
| **검증** | 329 passed |

### M-5. ML Feature NaN 무조건 0 대체

| 항목 | 내용 |
|------|------|
| **버그** | `build_training_dataset()`에서 `np.nan_to_num(X, nan=0.0)` — 결측치와 정상 0값 구분 불가 |
| **영향** | ML 모델이 결측치를 정상값으로 오인 → 잘못된 예측 |
| **수정** | per-feature mean 대체로 변경 (`builder.py:292-296`) |
| **검증** | 329 passed |

---

## 🟢 이미 구현되어 수정 불필요

| 항목 | 증명 |
|------|------|
| **백테스트 거래 비용/슬리피지** | `BUY_FEE_RATE=0.00015`, `SELL_FEE_RATE=0.00015`, `SELL_TAX_RATE=0.0023`, `_slippage()` 메서드 모두 구현 완료 (`engine.py:162-181, 184-187`) |
| **KOSPI TR_ID output2 fallback** | `parse_index_history()`: `raw.get("output2") or raw.get("output") or []` — 이미 fallback 구현 (`mapper.py:317`) |
| **PresetRouter HYSTERESIS_POINTS** | `HYSTERESIS_POINTS = 5` 클래스 변수는 이미 존재 — 구현만 누락되어 있던 상태 (`preset_router.py:50`) |
| **WebSocket 캐시 우선** | `_fetch_minute_prices()`에서 `_minute_builder` 캐시 먼저 확인 후 REST fallback — 이미 구현 완료 (`orchestrator.py:833-840`) |

---

## 수정 중 발생한 크리티컬 이슈

| # | 문제 | 발생 Phase | 원인 | 조치 |
|:-:|------|:---------:|------|------|
| 1 | `cannot import name 'MarketRegime' from 'core.types'` | P1-B9 | `core/types.py` 패치 시 `from core.config import ...` 라인 실수로 삭제 | 즉시 재추가 → 329 passed 복구 |
| 2 | `MockOrchestrator`에 `get_account_summary()` 없음 | P2-C4 | C-4 변경으로 bot이 `_broker` 대신 `get_account_summary()` 호출 | Mock에 메서드 추가 |
| 3 | `_refine_entry()` TypeError: `len(None)` | P2-C6 | `_fetch_minute_prices()`가 None 반환 → `refine_entry()`가 `len(None)` 호출 | None 체크 + fallback return 추가 |

## 파일별 변경 요약

| 파일 | 변경 | 관련 버그 |
|------|:----:|-----------|
| `main.py` | +181/-202 | C-6, H-4, H-5, H-7, C-2, H-2, M-4 |
| `live/orchestrator.py` | +30/-2 | C-4, H-3, H-6, H-2 |
| `live/executor.py` | +10/-3 | C-4 |
| `live/risk.py` | +11/-3 | H-1 |
| `data/universe_manager.py` | +39/-17 | C-1, C-2 |
| `data/real_data_provider.py` | +12/-3 | H-4, M-3 |
| `data/adapters/kis/client.py` | +17 | C-5 |
| `data/adapters/kis/token_manager.py` | +11/-4 | C-3 |
| `data/minute_bar_builder.py` | +12/-1 | M-1 |
| `data/repository.py` | +12 | H-3 |
| `core/config.py` | +6 | H-7, M-2 |
| `core/types.py` | +9/-1 | M-4 |
| `strategy/preset_router.py` | +11/-4 | C-3-hysteresis |
| `messaging/bot.py` | +13/-1 | H-6, D-3 |
| `ml/features/builder.py` | +6/-2 | M-5 |
| `backtest/engine.py` | +1 | D-4 (slippage 상수) |
| `tests/test_live/test_messaging_bot.py` | +3 | C-4 test mock |
| **18개 파일** | **+824/-259** | **22개 버그 수정** |
